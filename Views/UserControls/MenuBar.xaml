<UserControl x:Class="_365WPF.Views.UserControls.MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:_365WPF.Views.UserControls"
             mc:Ignorable="d" 
             Height="70" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Menu>
            <MenuItem Header="File" FontSize="16">
                <MenuItem Header="New" FontSize="16"/>
                <MenuItem Header="Save" FontSize="16"/>
                <MenuItem Header="Save As" FontSize="16"/>
                <MenuItem Header="Exit" FontSize="16"/>
            </MenuItem>
            <MenuItem Header="View" FontSize="16"/>
            <MenuItem Header="Help" FontSize="16">
                <MenuItem Header="User Manual" FontSize="16"/>
                <MenuItem Header="About" FontSize="16"/>
            </MenuItem>
        </Menu>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <Button Width="50" Margin="5" Grid.Column="0"/>
            <TextBox Width="150" Grid.Column="1" Margin="5"/>
        </Grid>
    </Grid>
</UserControl>
